module.exports = {
  restaurantCatalogEnabled: true,
  restaurantMenuEnabled: true,
  cartEnabled: true,
  ordersEnabled: true,
  adminEnabled: true,
  riderEnabled: true,
  restaurantOwnerEnabled: true,
}

// Local demo toggle:
// Set any of the flags above to false if you want to disable that feature locally.
// This is safe to undo by switching them back to true.
